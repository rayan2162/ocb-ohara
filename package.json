{
  "name": "ocs-ohara",
  "version": "1.0.0",
  "private": true,
  "description": "Static course catalog for Ohara — generate courses-index.json from courses/*.json and serve the result on GitHub Pages.",
  "scripts": {
    "build": "node generate-index.js",
    "start": "node generate-index.js"
  }
}
